apiVersion: v1
kind: Service
metadata:
  name: {{ include "fleetops.fullname" . }}
  labels:
    {{- include "fleetops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "fleetops.selectorLabels" . | nindent 4 }}
